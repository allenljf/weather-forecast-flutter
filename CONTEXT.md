# CONTEXT

本專案的領域詞彙表。這份檔案只定義「我們在講什麼」，不描述「怎麼實作」。
當程式碼、issue、commit、文件中出現領域概念時，一律使用這裡的正式名稱。

詞彙隨拷問過程逐步確立，決策脈絡見 [docs/GRILL_LOG.md](docs/GRILL_LOG.md)。

---

## 查詢

### 縣市（Location）

天氣預報的唯一查詢單位。中央氣象署的今明 36 小時預報**只以縣市為粒度**，恰好 22 個，且官方名稱一律使用「臺」而非「台」。鄉鎮區不是縣市，不在本專案範圍內。

> 不要用「城市」「地區」「地點」稱呼它。需求文件中的「城市名稱」指的就是縣市。

### 查詢字串（Query）

使用者在輸入框中實際打進去的原始文字，未經任何處理。它**不等於**縣市——「台北」「臺北市 」「Taipei」都是合法的查詢字串，但只有其中一部分能對應到縣市。

### 正規化（Normalisation）

把查詢字串轉換成縣市正式名稱的過程。這個轉換有可能失敗。

### 別名（Alias）

能被正規化對應到某個縣市的非正式寫法，例如「台北」「台北市」都是「臺北市」的別名。別名是本專案自行定義的，不是氣象署提供的。

### 無效查詢（Invalid query）

正規化失敗的查詢字串，也就是無法對應到任何縣市的輸入。這是需求明確要求要處理的情況之一。

---

## 預報資料

### 預報時段（Forecast slot）

一段有明確起訖時間的預報區間。今明 36 小時預報恆為 **3 個時段**，但每段長度**不相等**：只有第二、三段是整齊的 12 小時，第一段只涵蓋當前區塊的剩餘時間。因此「36 小時」是涉蓋時數的**上限**，不是恆等式。（實測所得，見 [docs/GRILL_LOG.md](docs/GRILL_LOG.md) Q4 的實作期修正）

### 天氣要素（Weather element）

一個預報時段內的某一種觀測面向。本專案使用的五種：

| 要素 | 正式名稱 | 說明 |
| --- | --- | --- |
| `Wx` | 天氣現象 | 例如「多雲時晴」 |
| `PoP` | 降雨機率 | 百分比 |
| `MinT` | 最低溫度 | 攝氏 |
| `MaxT` | 最高溫度 | 攝氏 |
| `CI` | 舒適度 | 例如「舒適」 |

### 天氣分類代碼（Weather code）

氣象署替每一句天氣現象描述指定的分類編號。數百句中文描述會收斂到少數幾十個代碼，因此**代碼本身就是官方的粗分類**，不是本專案自己歸納出來的。

---

## 狀態與失敗

### 畫面狀態（View state）

顯示區塊在任一時刻所處的情況。需求明定**恰好四種**：

1. **初始** — 使用者尚未送出任何查詢。
2. **讀取中** — 查詢進行中。不使用 Dialog 呈現。
3. **氣象資料** — 查詢成功且有資料可顯示。
4. **錯誤** — 查詢未能產出可顯示的氣象資料。

> 「查無資料」屬於**錯誤**，不是獨立的第五種狀態。

### 失敗（Failure）

導致畫面進入「錯誤」狀態的具體原因。失敗是**被窮舉列舉**的，不是一段自由文字——每一種失敗都有自己的身分與對應文案。

### 授權碼（Token）

呼叫氣象署 API 所需的個人金鑰。它是**組態**而非領域資料：缺少授權碼是設定錯誤，會在程式啟動時就中止，不會變成上述四種畫面狀態中的任何一種。
